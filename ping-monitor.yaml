apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: resilient-ping-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: resilient-ping
  endpoints:
  - port: http
    path: /metrics
    interval: 15s